import { Button } from '@/components/ui/button';

const Dashboard = () => {
   return (
      <div>
         <Button
            variant='secondary'
            size={'lg'}
         >
            Dashboard
         </Button>
      </div>
   );
};

export default Dashboard;
